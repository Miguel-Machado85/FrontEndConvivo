import { Routes } from '@angular/router';
import { ConfirmComponent } from './confirm/confirm.component';

export const ConfirmarRoutes: Routes = [
  {
    path: 'confirm',
    component: ConfirmComponent,
  },
];